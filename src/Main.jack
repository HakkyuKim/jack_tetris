class Main {
    function void main() {
        var GameBoard gameBoard;
        var Runner runner;
        let gameBoard = GameBoard.new(10, 20, 1);
        let runner = Runner.new(gameBoard);
        do runner.run();
        return;
    }
}